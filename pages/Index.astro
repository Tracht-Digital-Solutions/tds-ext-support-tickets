---
// Tickets page, injected into the host build via the manifest's route slot.
// Checkpoint-1: shell + the list island. The full board (detail view, comment
// thread, new-ticket form) is built in the next frontend checkpoint.
import TicketList from "../islands/TicketList.tsx";
---

<section class="page page--tickets">
  <h1>Support-Tickets</h1>
  <TicketList client:load />
</section>
